export const current = { withinClick: false };
if (globalThis.window) {
    globalThis.window.addEventListener("click", () => (current.withinClick = true), true);
    globalThis.window.addEventListener("click", () => (current.withinClick = false));
}
//# sourceMappingURL=clickListener.js.map